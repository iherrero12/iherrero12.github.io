---
export interface Props {
	title: string;
	body: string;
	icon: string;
}

const { icon, title, body } = Astro.props;
---

<li class="skill-card">
	<i class={icon}></i>
	<h2>
		{title}
	</h2>
	<p>
		{body}
	</p>
</li>
<style>
	.skill-card {
		list-style: none;
		display: flex;
		flex-direction: column;
		align-items: center;
		max-width: 280px;
		padding: 0.75rem;
		background-color: rgba(255, 255, 255, 0.05);
		background-image: none;
		background-size: 400%;
		border-radius: 0.6rem;
		background-position: 100%;
		transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
		box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
	}
	h2 {
		color: rgb(255, 170, 0);
		margin: 0;
		font-size: 1.25rem;
		transition: color 0.6s cubic-bezier(0.22, 1, 0.36, 1);
	}
	p {
		margin-top: 0.5rem;
		margin-bottom: 0;
		color: rgba(240, 240, 240, 0.87);
		text-align: center;
	}
	.skill-card:is(:hover, :focus-within) {
		background-position: 0;
		background-image: var(--accent-gradient);
	}
	.skill-card:is(:hover, :focus-within) h2 {
		color: lightskyblue;
	}
	i {
		margin-bottom: 1rem;
		font-size: 3em;
		color: rgb(255, 170, 0);
	}
</style>
